class Student:    def __init__(self, name, roll_number, marks):        self.name = name        self.roll_number = roll_number        self.marks = int(marks)        def get_grade(self):        if self.marks >= 90:            return 'A'        elif self.marks >= 80 and self.marks < 90:            return 'B'        elif self.marks >= 70 and self.marks < 80:            return 'C'        else:            return 'D'        class GradeBook:    def __init__(self):        self.students = []    def add_student(self, name, roll_number, marks):        student = Student(name, roll_number, marks)        self.students.append(student)    def display_students(self):        for i in self.students:            grade = i.get_grade()            print(f"Name: {i.name}, Roll Number: {i.roll_number}, Marks: {i.marks}, Grade: {grade}")    def calculate_average(self):        s = 0        for i in self.students:            s += i.marks        avg = s/len(self.students)        print(f"Average Marks: {avg:.2f}")grade_book = GradeBook()while True:    try:        s = input().split()        if s == []:            break        if s[0] == 'Add':            grade_book.add_student(s[2][:-1], s[3][:-1], s[4])        elif s[0] == 'DisplayStudents':            grade_book.display_students()        elif s[0] == 'CalculateAverageMarks':            grade_book.calculate_average()    except:        break