class Items:    def __init__(self, name, price, quantity):        self.name = name        self.price = int(price)        self.quantity = int(quantity)        # print(self.name, self.price, self.quantity)    def update_Quantity(self, quantity):        passclass Cart:    def __init__(self):        self.items = {}            def Add_items(self, name, price, quantity):        if name not in self.items:            self.items[name] = [int(price), int(quantity)]            return f"Item added to cart: {name}"        else:            # self.items[name][0] += int(price)            self.items[name][1] += int(quantity)        return f"Quantity updated for item: {name}"                          def Remove_item(self, name):        # for name in self.items.items():            # print(k,v)        if name in self.items:            # print(name)            self.items.pop(name)            return f"Item removed from cart: {name}"        else:            return f"Item not found in the cart: {name}"            def Display_item(self):        if len(self.items) == 0:            return "Cart is empty."        else:            complete_cart = "Items in Cart:\n"            cart_total = 0.0            for k, v in self.items.items():                item_total = float(v[0]) * float(v[1])                cart_total += item_total                complete_cart += f"{k} - ${float(v[0]):.2f} * {int(v[1])} = ${item_total:.2f}\n"                        complete_cart += f"Total Amount: ${cart_total:.2f}"            return complete_cart.strip()                # def Calculate_total_amount(self):    #     pass    def Apply_discount(self):        total_amount = sum(float(v[0]) * int(v[1]) for v in self.items.values())        discount = total_amount * 0.10        final_amount = total_amount - discount        return f"Total after 10% discount: ${final_amount:.2f}"c = Cart()while True:    try:        s = input().split()        if s == []:            break        if s[0] == 'Add':            print(c.Add_items(s[2][:-1], s[3][:-1], s[4]))                elif s[0] == 'Remove':            print(c.Remove_item(s[2]))        elif s[0] == 'DisplayCart':            print(c.Display_item())                elif s[0] == 'ApplyDiscount':            print(c.Apply_discount())    except:        break